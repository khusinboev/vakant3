/* eslint-disable react-refresh/only-export-components -- the kit ships helpers next to their component. */
/**
 * Moved to the v3 kit (`ui/JsonDetails`) — it is used by audit, errors and the
 * auto-post scheduler. Kept as a re-export for the unmigrated pages.
 */
export { default, type JsonDetailsProps } from "../ui/JsonDetails";
