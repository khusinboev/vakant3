/* eslint-disable react-refresh/only-export-components -- the kit ships helpers next to their component. */
/**
 * Moved to the v3 kit (96px, 13px label). Kept as a re-export so pages that
 * have not migrated yet keep compiling — import from `../ui` in new code.
 */
export { default, type EmptyStateProps } from "../ui/EmptyState";
